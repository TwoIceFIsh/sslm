"use client"
import React from 'react';
import {toast} from "sonner";

const Alert = () => {
    toast.success('Dooray API Token 저장 성공')
    return (
        <div>

        </div>
    );
};

export default Alert;